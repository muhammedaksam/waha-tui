export const DEFAULTS = {
  SESSION_NAME: "default",
} as const

export const TIME_MS = {
  CONFIG_SUCCESS_DELAY: 1500,

  WS_DEBOUNCE: 500,
  WS_RECONNECT_BASE_DELAY: 1000,
  WS_MAX_RECONNECT_DELAY: 30000,

  NETWORK_MONITOR_INTERVAL: 30000,

  UPDATE_CHECK_CACHE_DURATION: 24 * 60 * 60 * 1000,
  CACHE_DEFAULT_TTL: 5 * 60 * 1000,

  SESSION_CONTACT_MAP_TTL: 120000,
  SESSION_CHATS_CACHE_TTL: 30000,

  SEND_MESSAGE_RELOAD_DELAY: 100,

  QR_SESSION_READY_MAX_WAIT: 30000,
  QR_SESSION_READY_POLL_INTERVAL: 1000,
  QR_REFRESH_INTERVAL: 15000,
  QR_STATUS_CHECK_INTERVAL: 1000,

  TYPING_PAUSE_DELAY: 2000,

  TOAST_DEFAULT_AUTO_DISMISS: 5000,
  TOAST_ERROR_DURATION: 5000,
  TOAST_WARNING_DURATION: 4000,
  TOAST_SUCCESS_DURATION: 3000,
  TOAST_INFO_DURATION: 3000,

  PRESENCE_RESUBSCRIBE_INTERVAL: 5 * 60 * 1000,
  PRESENCE_INACTIVITY_TIMEOUT: 30000,
  PRESENCE_ACTIVITY_CHECK_INTERVAL: 5000,
} as const

export const TIME_S = {
  PIN_MESSAGE_DEFAULT_DURATION: 604800,
} as const
